describe('Evidence Collection', () => {
  it('should upload a file and return evidence metadata', async () => {
    // TODO: multipart/form-data POST, assert metadata stored in DB
    expect(true).toBe(true);
  });

  it('should attach investigation notes', async () => {
    expect(true).toBe(true);
  });

  it('should list evidence for an incident', async () => {
    expect(true).toBe(true);
  });
});
