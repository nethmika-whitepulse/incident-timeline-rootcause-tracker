describe('Dashboard', () => {
  it('should return open incident count', async () => {
    // TODO: GET /api/dashboard and assert openCount field
    expect(true).toBe(true);
  });

  it('should return incidents grouped by severity', async () => {
    expect(true).toBe(true);
  });

  it('should return mean resolution time', async () => {
    expect(true).toBe(true);
  });

  it('should return recently closed incidents', async () => {
    expect(true).toBe(true);
  });
});
