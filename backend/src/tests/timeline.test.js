const request = require('supertest');

describe('Timeline Events', () => {
  it('should add a timeline event to an incident', async () => {
    // TODO: POST /api/timeline/:incidentId with timestamp + description
    expect(true).toBe(true);
  });

  it('should return events in chronological order', async () => {
    // TODO: insert events out of order, GET and assert sorted
    expect(true).toBe(true);
  });

  it('should update a timeline event', async () => {
    expect(true).toBe(true);
  });
});
