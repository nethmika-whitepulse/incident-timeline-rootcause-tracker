const express = require('express');
const router = express.Router();

// TODO: import controller and wire handlers
// const actionItemController = require('../controllers/actionItemController');

module.exports = router;
